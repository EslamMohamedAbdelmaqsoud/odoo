<odoo>

    <!-- Inherit the sale order form view to add the 'ref' field -->
    <record id="view_order_form_inherit" model="ir.ui.view">
        <field name="name">sale order form inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="before">
                <field name="ref" readonly="state != 'draft'"/>
            </field>
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="property_id"/>
            </xpath>

            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="credit" />
            </xpath>

            <!-- ِAdding new tab  -->
            <xpath expr="//notebook" position="inside">
                <page string="Edit 2026">
                    <group>
                        <group>
                            <field name="edit_2026"/>
                        </group>
                    </group>
                </page>
            </xpath>

        </field>
    </record>

    <!-- Inherit the sale order tree view to add the 'ref' field -->
    <record id="view_order_tree_inherit" model="ir.ui.view">
        <field name="name">Sale Order Tree Inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="ref" string="Customer Reference"/>
            </xpath>
        </field>
    </record>

</odoo>